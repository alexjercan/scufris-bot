# Port weather/web_search/calculator/datetime tools to opencode

- STATUS: OPEN
- PRIORITY: 75
- TAGS: tools,opencode

Migrate the existing `@tool`-based implementations (`weather_tool`,
`web_search`, `calculator`, `datetime_tool`) to opencode's tool schema.
Functional parity with v1; no behaviour changes beyond the wrapper.

Spun off from scufris-v2 planning doc (`tasks/20260613-085701/TASK.md`).

