# NixOS module + hardened systemd unit

- STATUS: OPEN
- PRIORITY: 60
- TAGS: nix,systemd,deploy

Port the v1 NixOS module (`20260510-192748`) to the v2 server:
DynamicUser, hardening, VM test. Targets `scufris-server-v2` once the
flake task lands.

Spun off from scufris-v2 planning doc (`tasks/20260613-085701/TASK.md`).

