# Secrets/config injection guide (env-file / sops-nix / agenix)

- STATUS: OPEN
- PRIORITY: 30
- TAGS: deploy,security,docs

Port the v1 secrets doc (`20260510-192923`), adjusted for opencode's
own config / secrets if any. Document env-file vs. sops-nix vs.
agenix; recommend one per deployment shape.

Spun off from scufris-v2 planning doc (`tasks/20260613-085701/TASK.md`).

