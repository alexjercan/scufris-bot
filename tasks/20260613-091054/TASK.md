# History compaction strategy for opencode context windows

- STATUS: OPEN
- PRIORITY: 35
- TAGS: memory,compaction

Summary + facts layers if opencode's own context management isn't
sufficient on its own. Equivalent to v1's compaction Phase 1 / 2 / 3
work; only required if the session-model spike shows opencode's
defaults aren't enough.

Spun off from scufris-v2 planning doc (`tasks/20260613-085701/TASK.md`).

