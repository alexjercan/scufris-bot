# Persistent conversation/session store (SQLite)

- STATUS: OPEN
- PRIORITY: 60
- TAGS: memory,persistence

If opencode doesn't persist sessions long-term, build our own SQLite
store + export (md/json), as in v1 task `20260520-145229`. Whether this
is needed at all depends on the outcome of the session-model spike.

Spun off from scufris-v2 planning doc (`tasks/20260613-085701/TASK.md`).

